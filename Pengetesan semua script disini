local Players = game:GetService("Players")
local LocalPlayer = Players.LocalPlayer
local PlayerGui = LocalPlayer:WaitForChild("PlayerGui")
local StarterGui = game:GetService("StarterGui")

local function clearCustomAC()
    -- Game 1: PVEye Custom AC
    local Tracker = PlayerGui:FindFirstChild("Tracker")
    local Loop = PlayerGui:FindFirstChild("Loop")
    if Tracker then Tracker:Destroy() end
    if Loop then Loop:Destroy() end

    -- Game 2: OPRW Custom AC
    local anti = PlayerGui:FindFirstChild("anti")
    if anti then anti:Destroy() end
    
    local starterAnti = StarterGui:FindFirstChild("anti")
    if starterAnti then starterAnti:Destroy() end
end

-- Clear immediately on execute
clearCustomAC()

-- Clear on regeneration/spawn
PlayerGui.ChildAdded:Connect(function(child)
    if child.Name == "Tracker" or child.Name == "Loop" or child.Name == "anti" then
        task.spawn(function()
            local t = PlayerGui:FindFirstChild("Tracker")
            local l = PlayerGui:FindFirstChild("Loop")
            local a = PlayerGui:FindFirstChild("anti")
            if t then t:Destroy() end
            if l then l:Destroy() end
            if a then a:Destroy() end
        end)
    end
end)

-- Clear WWE/Rumble Tool AC Scripts when tools are added
local function handleToolAC(tool)
    if tool:IsA("Tool") then
        if tool.Name == "WWE Clean" then
            local acScript = tool:WaitForChild("LocalScript", 2)
            if acScript then acScript:Destroy() end
        end
    end
end

LocalPlayer.Character.ChildAdded:Connect(handleToolAC)
LocalPlayer.Backpack.ChildAdded:Connect(handleToolAC)

for _, tool in ipairs(LocalPlayer.Backpack:GetChildren()) do
    handleToolAC(tool)
end
if LocalPlayer.Character then
    for _, tool in ipairs(LocalPlayer.Character:GetChildren()) do
        handleToolAC(tool)
    end
end




-- Load Rayfield UI Library
local Rayfield = loadstring(game:HttpGet('https://sirius.menu/rayfield'))()

-- Global Variables / Configurations
local ReachEnabled = false
local ReachDistance = 15
local ReachType = "Sphere"
local UseCBring = false
local UseFTI = false
local VisualizerEnabled = false
local VisualizerColor = Color3.fromRGB(0, 170, 255)
local VisualizerTransparency = 0.5

local AutoClickerEnabled = false
local AutoClickerDelay = 0.1

local AimbotEnabled = false
local AimbotActive = false
local AimbotPart = "Head"
local AimbotTargetMode = "Distance"
local AimbotMode = "Hold" -- "Hold" or "Toggle"
local AimbotSmoothness = 0.2
local AimbotFOV = 150
local AimbotKeybind = Enum.KeyCode.E

local TankModeEnabled = false

local UIKey = Enum.KeyCode.RightShift

-- Services
local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local UserInputService = game:GetService("UserInputService")
local LocalPlayer = Players.LocalPlayer

-- Initialize Main Window
local Window = Rayfield:CreateWindow({
    Name = "Untitled OPRW // Private Training",
    LoadingTitle = "Untitled OPRW // Private Training",
    LoadingSubtitle = "by mousey",
    Theme = "DarkBlue",
    DisableRayfieldPrompts = false,
    DisableBuildWarnings = false,
    ConfigurationSaving = {
        Enabled = true,
        FolderName = "Folders",
        FileName = "BoxingConfig"
    },
    KeySystem = false
})

-- Create Tabs
local TabReach = Window:CreateTab("Reach", 4483362458)
local TabCombat = Window:CreateTab("Combat", 4483362458)
local TabTank = Window:CreateTab("Tank & AC", 4483362458)
local TabSettings = Window:CreateTab("Settings", 4483362458)

-- Tab 1: Reach Settings
TabReach:CreateSection("Reach Controls")

TabReach:CreateToggle({
    Name = "Enable Reach",
    CurrentValue = false,
    Flag = "ReachToggle",
    Callback = function(Value)
        ReachEnabled = Value
    end
})

TabReach:CreateSlider({
    Name = "Reach Distance",
    Range = {1, 50},
    Increment = 1,
    Suffix = " Studs",
    CurrentValue = 15,
    Flag = "ReachDistSlider",
    Callback = function(Value)
        ReachDistance = Value
    end
})

TabReach:CreateDropdown({
    Name = "Reach Shape",
    Options = {"Sphere", "Line"},
    CurrentOption = {"Sphere"},
    MultipleOptions = false,
    Flag = "ReachShapeDropdown",
    Callback = function(Option)
        ReachType = Option[1]
    end
})

TabReach:CreateToggle({
    Name = "Use CBring (Client Bring Parts)",
    CurrentValue = false,
    Flag = "CBringToggle",
    Callback = function(Value)
        UseCBring = Value
    end
})

TabReach:CreateToggle({
    Name = "Use FTI (Fast TouchInterest)",
    CurrentValue = false,
    Flag = "FTIToggle",
    Callback = function(Value)
        UseFTI = Value
    end
})

TabReach:CreateSection("Visualizer")

TabReach:CreateToggle({
    Name = "Enable Visualizer",
    CurrentValue = false,
    Flag = "VisualizerToggle",
    Callback = function(Value)
        VisualizerEnabled = Value
    end
})

TabReach:CreateColorPicker({
    Name = "Visualizer Color",
    Color = Color3.fromRGB(0, 170, 255),
    Flag = "VisualizerColorPicker",
    Callback = function(Value)
        VisualizerColor = Value
    end
})

TabReach:CreateSlider({
    Name = "Visualizer Transparency",
    Range = {0, 1},
    Increment = 0.1,
    Suffix = "",
    CurrentValue = 0.5,
    Flag = "VisualizerTransSlider",
    Callback = function(Value)
        VisualizerTransparency = Value
    end
})


-- Tab 2: Combat Settings (Aimbot & Autoclicker)
TabCombat:CreateSection("Aimbot Controls")

TabCombat:CreateToggle({
    Name = "Enable Aimbot",
    CurrentValue = false,
    Flag = "AimbotToggle",
    Callback = function(Value)
        AimbotEnabled = Value
    end
})

TabCombat:CreateDropdown({
    Name = "Aimbot Target Part",
    Options = {"Head", "HumanoidRootPart"},
    CurrentOption = {"Head"},
    MultipleOptions = false,
    Flag = "AimbotPartDropdown",
    Callback = function(Option)
        AimbotPart = Option[1]
    end
})

TabCombat:CreateDropdown({
    Name = "Aimbot Target Mode",
    Options = {"Distance", "Mouse"},
    CurrentOption = {"Distance"},
    MultipleOptions = false,
    Flag = "AimbotModeDropdown",
    Callback = function(Option)
        AimbotTargetMode = Option[1]
    end
})

TabCombat:CreateDropdown({
    Name = "Aimbot Trigger Mode",
    Options = {"Hold", "Toggle"},
    CurrentOption = {"Hold"},
    MultipleOptions = false,
    Flag = "AimbotTriggerDropdown",
    Callback = function(Option)
        AimbotMode = Option[1]
        AimbotActive = false
    end
})

TabCombat:CreateSlider({
    Name = "Aimbot Smoothness",
    Range = {0.1, 1},
    Increment = 0.05,
    Suffix = "",
    CurrentValue = 0.2,
    Flag = "AimbotSmoothSlider",
    Callback = function(Value)
        AimbotSmoothness = Value
    end
})

TabCombat:CreateSlider({
    Name = "Aimbot Mouse FOV",
    Range = {30, 500},
    Increment = 10,
    Suffix = " px",
    CurrentValue = 150,
    Flag = "AimbotFOVSlider",
    Callback = function(Value)
        AimbotFOV = Value
    end
})

TabCombat:CreateKeybind({
    Name = "Aimbot Activation Key",
    CurrentKeybind = "E",
    HoldToInteract = false,
    Flag = "AimbotBind",
    Callback = function(Keybind)
        AimbotKeybind = Keybind
        AimbotActive = false
    end
})

TabCombat:CreateSection("Auto Clicker")

TabCombat:CreateToggle({
    Name = "Enable Auto Clicker",
    CurrentValue = false,
    Flag = "ACToggle",
    Callback = function(Value)
        AutoClickerEnabled = Value
    end
})

TabCombat:CreateSlider({
    Name = "Click Delay",
    Range = {0.01, 1},
    Increment = 0.01,
    Suffix = " sec",
    CurrentValue = 0.1,
    Flag = "ACDelaySlider",
    Callback = function(Value)
        AutoClickerDelay = Value
    end
})


-- Tab 3: Tank Mode & Bypass Status
TabTank:CreateSection("Tank Mode")

local function updateTankMode()
    local myChar = LocalPlayer.Character
    if not myChar then return end
    for _, part in ipairs(myChar:GetDescendants()) do
        if part:IsA("BasePart") and part.Name ~= "HumanoidRootPart" then
            part.CanTouch = not TankModeEnabled
        end
    end
end

TabTank:CreateToggle({
    Name = "Enable Tank Mode (CanTouch Bypass)",
    CurrentValue = false,
    Flag = "TankToggle",
    Callback = function(Value)
        TankModeEnabled = Value
        updateTankMode()
    end
})

TabTank:CreateSection("Anti-Cheat Bypass Status")
TabTank:CreateParagraph({Title = "PVEye Bypass", Content = "Active (Instance-Based Tracker & Loop Autodestroy)"})
TabTank:CreateParagraph({Title = "WWE Tool Bypass", Content = "Active (Tool LocalScript Autodestroy)"})


-- Tab 4: Menu Settings
TabSettings:CreateSection("Menu Keybinds")

TabSettings:CreateKeybind({
    Name = "Toggle Menu Key",
    CurrentKeybind = "RightShift",
    HoldToInteract = false,
    Flag = "MenuBind",
    Callback = function(Keybind)
        UIKey = Keybind
    end
})


-- Reach Helper functions
local function getDamagingPart()
    local myChar = LocalPlayer.Character
    if not myChar then return nil end
    local tool = myChar:FindFirstChildOfClass("Tool")
    if tool then
        return tool:FindFirstChild("Handle") or tool:FindFirstChildOfClass("BasePart")
    end
    -- Fallback to character hands/arms for toolless boxing
    return myChar:FindFirstChild("Right Hand") or myChar:FindFirstChild("Right Arm") or
           myChar:FindFirstChild("Left Hand") or myChar:FindFirstChild("Left Arm") or
           myChar:FindFirstChild("HumanoidRootPart")
end

-- Initialize Visualizer Part
local visualizer = Instance.new("Part")
visualizer.Material = Enum.Material.ForceField
visualizer.CanCollide = false
visualizer.Anchored = true
visualizer.CastShadow = false
visualizer.Transparency = VisualizerTransparency
visualizer.Color = VisualizerColor
visualizer.BottomSurface = Enum.SurfaceType.Smooth
visualizer.TopSurface = Enum.SurfaceType.Smooth

-- Visualizer Loop
RunService.RenderStepped:Connect(function()
    if ReachEnabled and VisualizerEnabled then
        local dp = getDamagingPart()
        if dp then
            visualizer.Parent = workspace
            visualizer.Color = VisualizerColor
            visualizer.Transparency = VisualizerTransparency
            
            if ReachType == "Sphere" then
                visualizer.Shape = Enum.PartType.Ball
                visualizer.Size = Vector3.new(ReachDistance, ReachDistance, ReachDistance)
                visualizer.CFrame = dp.CFrame
            else -- Line
                visualizer.Shape = Enum.PartType.Block
                visualizer.Size = Vector3.new(1, 1, ReachDistance)
                visualizer.CFrame = dp.CFrame * CFrame.new(0, 0, -(ReachDistance / 2))
            end
        else
            visualizer.Parent = nil
        end
    else
        visualizer.Parent = nil
    end
end)

-- Main Reach Execution Loop (RenderStepped)
RunService.RenderStepped:Connect(function()
    if not ReachEnabled then return end
    
    local myChar = LocalPlayer.Character
    local myHRP = myChar and myChar:FindFirstChild("HumanoidRootPart")
    if not myHRP then return end
    
    local dp = getDamagingPart()
    if not dp then return end
    
    pcall(function()
        for _, targetPlayer in ipairs(Players:GetPlayers()) do
            if targetPlayer ~= LocalPlayer and targetPlayer.Character then
                local tChar = targetPlayer.Character
                local tHRP = tChar:FindFirstChild("HumanoidRootPart")
                local tHum = tChar:FindFirstChildOfClass("Humanoid")
                
                if tHRP and tHum and tHum.Health > 0 then
                    local dist = (tHRP.Position - myHRP.Position).Magnitude
                    if dist <= ReachDistance then
                        -- 1. CBring Teleportation
                        if UseCBring then
                            local partsToBring = {
                                tChar:FindFirstChild("Left Arm"),
                                tChar:FindFirstChild("LeftLowerArm"),
                                tChar:FindFirstChild("LeftHand"),
                                tChar:FindFirstChild("Right Arm"),
                                tChar:FindFirstChild("RightLowerArm"),
                                tChar:FindFirstChild("RightHand"),
                                tChar:FindFirstChild("Torso"),
                                tChar:FindFirstChild("UpperTorso"),
                                tChar:FindFirstChild("LowerTorso")
                            }
                            for _, part in ipairs(partsToBring) do
                                if part then
                                    part.CFrame = myHRP.CFrame * CFrame.new(0, 0, -3.5)
                                end
                            end
                        end
                        
                        -- 2. FTI (Fast TouchInterest) Simulation
                        if UseFTI then
                            for _, child in ipairs(tChar:GetChildren()) do
                                if child:IsA("BasePart") then
                                    firetouchinterest(child, dp, 0)
                                    firetouchinterest(child, dp, 1)
                                end
                            end
                        end
                    end
                end
            end
        end
    end)
end)

-- Aimbot Target Acquisition
local function getAimbotTarget()
    local target = nil
    local minVal = math.huge
    local myChar = LocalPlayer.Character
    local myHRP = myChar and myChar:FindFirstChild("HumanoidRootPart")
    if not myHRP then return nil end

    local Camera = workspace.CurrentCamera
    pcall(function()
        for _, player in ipairs(Players:GetPlayers()) do
            if player ~= LocalPlayer and player.Character then
                local tChar = player.Character
                local tHRP = tChar:FindFirstChild("HumanoidRootPart")
                local tHum = tChar:FindFirstChildOfClass("Humanoid")
                if tHRP and tHum and tHum.Health > 0 then
                    if AimbotTargetMode == "Distance" then
                        local dist = (tHRP.Position - myHRP.Position).Magnitude
                        if dist < minVal then
                            minVal = dist
                            target = tChar:FindFirstChild(AimbotPart) or tHRP
                        end
                    elseif AimbotTargetMode == "Mouse" then
                        local pos, onScreen = Camera:WorldToViewportPoint(tHRP.Position)
                        if onScreen then
                            local mousePos = UserInputService:GetMouseLocation()
                            local dist = (Vector2.new(pos.X, pos.Y) - mousePos).Magnitude
                            if dist < minVal and dist <= AimbotFOV then
                                minVal = dist
                                target = tChar:FindFirstChild(AimbotPart) or tHRP
                            end
                        end
                    end
                end
            end
        end
    end)
    return target
end

-- Aimbot Thread Loop
RunService.RenderStepped:Connect(function()
    if AimbotEnabled and AimbotActive then
        local target = getAimbotTarget()
        if target then
            local Camera = workspace.CurrentCamera
            local currentCFrame = Camera.CFrame
            local targetLook = CFrame.new(Camera.CFrame.Position, target.Position)
            Camera.CFrame = currentCFrame:Lerp(targetLook, AimbotSmoothness)
        end
    end
end)

-- Aimbot Input Handlers
UserInputService.InputBegan:Connect(function(input, gpe)
    if gpe then return end
    if input.KeyCode == AimbotKeybind then
        if AimbotMode == "Hold" then
            AimbotActive = true
        else
            AimbotActive = not AimbotActive
        end
    end
end)

UserInputService.InputEnded:Connect(function(input, gpe)
    if gpe then return end
    if input.KeyCode == AimbotKeybind then
        if AimbotMode == "Hold" then
            AimbotActive = false
        end
    end
end)

-- Auto Clicker Thread Loop
task.spawn(function()
    while true do
        task.wait(AutoClickerDelay)
        if AutoClickerEnabled then
            pcall(function()
                local myChar = LocalPlayer.Character
                local myTool = myChar and myChar:FindFirstChildOfClass("Tool")
                if myTool then
                    myTool:Activate()
                end
                
                -- Universal mouse click simulation for toolless systems
                local vu = game:GetService("VirtualUser")
                vu:CaptureController()
                vu:ClickButton1(Vector2.new(0, 0))
            end)
        end
    end
end)

-- Tank Mode State Keep-Alive Loops
LocalPlayer.CharacterAdded:Connect(function()
    task.wait(0.5)
    if TankModeEnabled then
        updateTankMode()
    end
end)

task.spawn(function()
    while true do
        task.wait(0.5)
        if TankModeEnabled then
            updateTankMode()
        end
    end
end)

-- Menu Hide/Show Keybind
UserInputService.InputBegan:Connect(function(input, gpe)
    if gpe then return end
    if input.KeyCode == UIKey then
        local RayfieldGui = game:GetService("CoreGui"):FindFirstChild("Rayfield") or LocalPlayer.PlayerGui:FindFirstChild("Rayfield")
        if RayfieldGui then
            RayfieldGui.Enabled = not RayfieldGui.Enabled
        end
    end
end)
